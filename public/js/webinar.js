
function showHide(){
	//if user has signed up already, show registered div
	/*if (user has already signed up) {
		$("#notRegistered").hide();
	};
	else {
		$("#registered").show();
	};*/


	//if user has not signed up, show notRegistered div 
	if (user is not signed up) {
		$("#notRegistered").show();
	};
	else{
		$("#registered").hide();
	};	
}
